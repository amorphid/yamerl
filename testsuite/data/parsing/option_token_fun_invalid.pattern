%% vim:ft=erlang:

%% TEST
?_assertThrow(
  {yamerl_the_fork_exception, [
      {yamerl_the_fork_invalid_option,error,
        "Invalid value for option \"token_fun\": it must be a function taking the next token as its sole argument, or the atom 'acc' or 'drop'",
        {token_fun,invalid}}
    ]
  },
  yamerl_the_fork_parser:string(<<>>, [{token_fun, invalid}])
)
