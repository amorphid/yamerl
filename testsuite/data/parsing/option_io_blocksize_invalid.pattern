%% vim:ft=erlang:

%% TEST
?_assertThrow(
  {yamerl_the_fork_exception, [
      {yamerl_the_fork_invalid_option,error,
        "Invalid value for option \"io_blocksize\": it must be a positive interger, expressed in bytes",
        {io_blocksize,invalid}}
    ]
  },
  yamerl_the_fork_parser:file("${FILENAME}", [{io_blocksize, invalid}])
)
